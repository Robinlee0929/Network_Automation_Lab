OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
NETWORK_AI_PROVIDER_DEMO_ENABLED=0
# Legacy/general workbenches are separate from NETWORK_AI_PROVIDER_DEMO_ENABLED.
# Set the legacy flag to exactly 1 only for intentional local provider use.
# OPENAI_API_KEY remains server-side provider credential configuration, not feature authorization.
LEGACY_AI_PROVIDER_ENABLED=0
