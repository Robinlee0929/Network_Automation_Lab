# v0.1 Portfolio Checklist

## Reviewer Path

- Open `README.md` for project scope, devices, safety model, and Day1-Day26 progress.
- Open `docs/portfolio_evidence/v0.1_release_notes.md` for release scope and known notes.
- Run `python network_lab.py --list-tasks --verbose` to review task metadata and safety labels.
- Run `python network_lab.py --report-index` to refresh local report visibility.
- Run `python network_lab.py --portfolio-finalize` to refresh the portfolio evidence index.
- Run `python network_lab.py --task demo-flow` to refresh the RC walkthrough.

## Portfolio Polish Checks

- README progress table includes Day25 and Day26.
- Safety labels stay explicit across report-only, read-only, guarded-live, dry-run, and disabled tasks.
- Dashboard `/reports` remains read-only and uses safe report links/redacted JSON preview.
- WireGuard runner remains dry-run by default and requires explicit live authorization.
- Generated `reports/`, caches, exports, real configs, and secrets remain ignored.
- Release notes and portfolio checklist are committed docs.
- Day26 adds no runner tasks, product features, or report generators.

## Evidence To Show

```text
docs/portfolio_evidence/day25_v0.1_rc_validation.md
docs/portfolio_evidence/v0.1_release_notes.md
docs/portfolio_evidence/v0.1_portfolio_checklist.md
reports/report_index.html
reports/portfolio/day19_runner_evidence_index.html
reports/portfolio/day24_rc_demo_flow.html
```

## Final Validation

Run the full local regression suite:

```powershell
python -m pytest --basetemp=.pytest-tmp-day26-release
```

Expected release posture:

- Full tests pass.
- No new runner task is present for Day26.
- No generated release package JSON/HTML is committed.
- No live device workflows are started by Day26 documentation changes.
